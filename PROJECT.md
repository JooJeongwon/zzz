# 📝 프로젝트 명세서: ZZZ (가칭)

> 한 줄 소개: 잠들거나 공부 중일 때, 나를 대신해 상태를 알리고 연인과 소통하는 AI 펫 & 상태 공유 플랫폼
> 

## 1. 프로젝트 개요 (Overview)

- **배경:** 연인이나 친구 사이에서 연락이 두절될 때(수면, 공부, 배터리 방전 등) 발생하는 불안감과 오해를 해소하고자 함.
- **목표:** 사용자의 상태(수면, 집중, 휴식 등)를 직관적으로 공유하고, 부재중일 때는 AI 에이전트가 사용자 말투로 상대방과 상호작용하여 '단절 없는 연결감'을 제공함.
- **핵심 가치:** 불안 해소(Peace of Mind), 감정적 연결(Emotional Connection), 재미(Gamification).

## 2. 타겟 오디언스 (Target Audience)

- **Main:** 연락 문제로 자주 다투거나 불안해하는 2030 커플.
- **Niche (특화 시장):**
    - **군화 & 곰신:** 휴대폰 사용 시간이 제한적인 군인 커플.
    - **장거리 연애 (Long D):** 시차가 있거나 물리적 거리가 먼 커플.
    - **수험생 & 고시생:** 공부 때문에 장시간 연락이 힘든 커플.
    - **교대 근무자:** 수면 패턴이 불규칙해 연락 타이밍이 안 맞는 커플.

## 3. 핵심 기능 (Functional Requirements)

### A. 상태 공유 시스템 (Status Sharing)

1. **원터치 위젯:** 홈 화면 위젯을 통해 '수면', '공부', '게임', '휴식' 등의 상태를 한 번에 설정.
2. **자동 감지 (Heartbeat Logic):**
    - 사용자가 별도 설정을 안 해도, 화면 꺼짐 및 움직임 없음이 일정 시간(예: 30분) 지속되면 서버에서 '수면 추정' 상태로 자동 전환.
    - 배터리 잔량 5% 미만 시 '방전 임박' 상태 자동 알림.
3. **기상/복귀 알림:** 사용자가 다시 폰을 켜거나(잠금 해제), 위젯을 터치하면 상대방에게 "OO님이 일어났어요/돌아왔어요" 푸시 알림 발송.

### B. AI 페르소나 챗봇 (AI Avatar & Chat)

1. **AI 대리 응답:** 사용자가 '부재중' 상태일 때 활성화. 상대방이 말을 걸면 AI 캐릭터가 답변.
2. **말투 복제 (Persona Cloning):** 평소 사용자의 채팅 데이터를 학습(RAG + LLM)하여 말투, 어휘, 이모티콘 사용 습관을 모방.
3. **상태 반영:** "나 지금 공부 중이라 2시간 뒤에 연락할게 ㅠㅠ"와 같이 현재 설정된 상태를 반영하여 답변.
4. **브리핑(Recap) 기능:** 사용자가 복귀했을 때, 부재중 동안 오고 간 AI와 상대방의 대화 내용을 요약해서 보고. ("자기는 동안 여친이 보고 싶다고 3번 말해서 달래줬어!")

### C. 시각적 피드백 (Visual & UX)

1. **캐릭터(Tamagotchi-like):** 현재 상태(자는 중, 책 읽는 중)를 귀여운 캐릭터 애니메이션으로 표현.
2. **상단 바/라이브 액티비티:** 상대방의 상태를 앱을 켜지 않고도 상단 바나 잠금 화면에서 확인 가능.

## 4. 기술적 아키텍처 (Technical Architecture)

### A. 클라이언트 (Client) - Flutter Hybrid

- **Cross-Platform Strategy:**
    - **UI & Logic:** Flutter (Dart)로 개발하여 안드로이드와 iOS 코드의 90% 이상을 공유 (생산성 증대).
    - **Native Bridge:** OS 특화 기능은 `MethodChannel`을 통해 네이티브 코드(Kotlin/Swift)와 통신.
- **백그라운드 제약 해결 (Native 영역):**
    - **Android:** `Foreground Service` (Kotlin)를 구현하여 Flutter 엔진과 독립적으로 Heartbeat 전송.
    - **iOS:** `BGTaskScheduler` 및 `ActivityKit` (Swift)을 활용하여 백그라운드 작업 및 라이브 액티비티 처리.
- **UI/UX:**
    - Flutter의 고성능 렌더링 엔진을 활용한 커스텀 캐릭터 애니메이션 및 반응형 UI.
    - **iOS 특화:** Live Activities(다이내믹 아일랜드)는 Swift로 별도 구현하여 연동.

### B. 백엔드 (Backend) - MSA 지향

- **Core Server:** 사용자 인증, 상태 관리, 하트비트 수신 및 로직 처리. (Spring Boot 또는 Node.js 추천)
- **AI Service:**
    - **Vector DB:** 사용자 채팅 로그 임베딩 저장 (Pinecone, Milvus 등).
    - **LLM Engine:** OpenAI API (GPT-4o/mini) 또는 경량화 모델(Llama 3) 서빙.
    - **Prompt Engineering:** 사용자별 페르소나 주입 및 금지어(민감 질문) 필터링 시스템.

### C. 데이터베이스 (DB)

- **RDB (MySQL/PostgreSQL):** 사용자 정보, 친구 관계, 정형 데이터.
- **Redis:** 실시간 상태 정보(Online/Offline/Sleep), 하트비트 캐싱 (속도 및 부하 분산).
- **NoSQL (MongoDB):** 채팅 로그 저장 및 AI 학습 데이터 원본.

## 5. 예상 리스크 및 대응 방안 (Risk Management)

- **AI 발언 실수:** AI가 헤어지자고 하거나 부적절한 답변을 할 위험.
    - *대응:* 민감한 키워드 감지 시 "이건 주인이 깨어나면 직접 답해야 할 것 같아요"라는 회피 로직(Fallback) 구현. AI 답변임을 시각적으로 명확히(배경색, 아이콘 등) 구분.
- **배터리 소모:** 백그라운드 로직 및 위젯 애니메이션으로 인한 과다 소모.
    - *대응:* 위젯 애니메이션 프레임 제한, 하트비트 주기 조절(가변적).